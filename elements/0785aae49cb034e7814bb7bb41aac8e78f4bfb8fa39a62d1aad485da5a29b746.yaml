"hash": |-
  0785aae49cb034e7814bb7bb41aac8e78f4bfb8fa39a62d1aad485da5a29b746
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     If the client supports only the default hash and signature algorithms
     (listed in this section), it MAY omit the signature_algorithms
     extension.  If the client does not support the default algorithms, or
     supports other hash and signature algorithms (and it is willing to
     use them for verifying messages sent by the server, i.e., server
     certificates and server key exchange), it MUST send the
"fixed": |-
  If the client supports only the default hash and signature algorithms (listed in this section), it MAY omit the signature_algorithms extension.  If the client does not support the default algorithms, or supports other hash and signature algorithms (and it is willing to use them for verifying messages sent by the server, i.e., server certificates and server key exchange), it MUST send the
"ko": |-
  클라이언트가 기본 해시 및 서명 알고리즘(이 섹션에 나열됨)만 지원하는 경우 서명_알고리즘 확장을 생략할 수 있습니다. 클라이언트가 기본 알고리즘을 지원하지 않거나 다른 해시 및 서명 알고리즘을 지원하는 경우(그리고 서버에서 보낸 메시지를 확인하는 데 이를 사용하려는 경우(예: 서버 인증서 및 서버 키 교환)) 반드시 다음을 보내야 합니다.
