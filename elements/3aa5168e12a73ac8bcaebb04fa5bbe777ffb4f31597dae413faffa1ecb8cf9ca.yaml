"hash": |-
  3aa5168e12a73ac8bcaebb04fa5bbe777ffb4f31597dae413faffa1ecb8cf9ca
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     Some example usage bounds for AES-GCM-SIV are given below.  The
     adversary's advantage is the "AdvEnc" from [key-derive] and is
     colloquially the ability of an attacker to distinguish ciphertexts
     from random bit strings.  The bounds below limit this advantage to
     2^-32.  For up to 256 uses of the same nonce and key (i.e., where one
     can assume that nonce misuse is no more than this bound), the
     following message limits should be respected (this assumes a short
     additional authenticated data (AAD), i.e., less than 64 bytes):
"fixed": |-
  Some example usage bounds for AES-GCM-SIV are given below.  The adversary's advantage is the "AdvEnc" from [key-derive] and is colloquially the ability of an attacker to distinguish ciphertexts from random bit strings.  The bounds below limit this advantage to 2^-32.  For up to 256 uses of the same nonce and key (i.e., where one can assume that nonce misuse is no more than this bound), the following message limits should be respected (this assumes a short additional authenticated data (AAD), i.e., less than 64 bytes):
"ko": |-
  AES-GCM-SIV의 몇 가지 사용 범위 예시는 다음과 같습니다. 적의 장점은 [key-derive]의 "AdvEnc"이며 구어적으로 공격자가 암호문을 임의의 비트 문자열과 구별할 수 있는 능력입니다. 아래 경계는 이 이점을 2^-32로 제한합니다. 동일한 nonce 및 키를 최대 256회 사용하는 경우(즉, nonce 오용이 이 범위를 넘지 않는다고 가정할 수 있는 경우) 다음 메시지 제한을 준수해야 합니다(이는 짧은 AAD(추가 인증 데이터)를 가정합니다. 즉, 64바이트 미만):
