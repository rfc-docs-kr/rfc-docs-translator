"hash": |-
  1d7c79123bd510740952a1a35c702500459bbd4778eca9a8aaeddcef3e0883cd
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     Note that because MD5 produces 16-byte outputs and SHA-1 produces
     20-byte outputs, the boundaries of their internal iterations will not
     be aligned.  Generating an 80-byte output will require that P_MD5
     iterate through A(5), while P_SHA-1 will only iterate through A(4).
"fixed": |-
  Note that because MD5 produces 16-byte outputs and SHA-1 produces 20-byte outputs, the boundaries of their internal iterations will not be aligned.  Generating an 80-byte output will require that P_MD5 iterate through A(5), while P_SHA-1 will only iterate through A(4).
"ko": |-
  MD5는 16바이트 출력을 생성하고 SHA-1은 20바이트 출력을 생성하므로 내부 반복의 경계가 정렬되지 않습니다. 80바이트 출력을 생성하려면 P_MD5가 A(5)를 통해 반복해야 하는 반면, P_SHA-1은 A(4)를 통해서만 반복해야 합니다.
