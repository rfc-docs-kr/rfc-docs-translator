"hash": |-
  dcafca864a2e51c8019acb82935b679c7a040cf8f90729bde4e43b25f8f5805e
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     15.  Let the Additional Authenticated Data encryption parameter be
          ASCII(Encoded Protected Header).  However, if a JWE AAD value is
          present (which can only be the case when using the JWE JSON
          Serialization), instead let the Additional Authenticated Data
          encryption parameter be ASCII(Encoded Protected Header || '.' ||
          BASE64URL(JWE AAD)).
"fixed": |-
  1.  Let the Additional Authenticated Data encryption parameter be ASCII(Encoded Protected Header).  However, if a JWE AAD value is present (which can only be the case when using the JWE JSON Serialization), instead let the Additional Authenticated Data encryption parameter be ASCII(Encoded Protected Header || '.' || BASE64URL(JWE AAD)).
"ko": |-
  1. 추가 인증 데이터 암호화 매개변수를 ASCII(인코딩된 보호 헤더)로 설정합니다. 그러나 JWE AAD 값이 있는 경우(JWE JSON 직렬화를 사용할 때만 해당) 대신 추가 인증 데이터 암호화 매개변수를 ASCII(인코딩된 보호 헤더 || '.' || BASE64URL(JWE AAD))로 설정합니다.
