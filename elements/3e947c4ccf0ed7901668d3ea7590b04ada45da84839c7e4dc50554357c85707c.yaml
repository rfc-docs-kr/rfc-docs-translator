"hash": |-
  3e947c4ccf0ed7901668d3ea7590b04ada45da84839c7e4dc50554357c85707c
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     This attribute is encoded as any other enum attribute syntax
     according to [RFC8010] as 32 bits.  However, all 32-bit enum values
     for this attribute MUST NOT exceed 0x00007fff, since these same
     values are also passed in two octets in the "operation-id" field (see
"fixed": |-
  This attribute is encoded as any other enum attribute syntax according to [RFC8010] as 32 bits.  However, all 32-bit enum values for this attribute MUST NOT exceed 0x00007fff, since these same values are also passed in two octets in the "operation-id" field (see
"ko": |-
  이 속성은 [RFC8010]에 따라 32비트로 다른 열거형 속성 구문으로 인코딩됩니다. 그러나 이 속성에 대한 모든 32비트 열거형 값은 0x00007fff를 초과해서는 안 됩니다. 왜냐하면 이러한 동일한 값은 "작업 ID" 필드의 두 옥텟으로도 전달되기 때문입니다(참조:
