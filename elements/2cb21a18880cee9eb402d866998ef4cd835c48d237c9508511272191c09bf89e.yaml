"hash": |-
  2cb21a18880cee9eb402d866998ef4cd835c48d237c9508511272191c09bf89e
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     2.  In version 1.5 of this document, T was defined as the BER
         encoding, rather than the DER encoding, of the DigestInfo value.
         In particular, it is possible -- at least in theory -- that the
         verification operation defined in this document (as well as in
         version 2.0) rejects a signature that is valid with respect to
         the specification given in PKCS #1 v1.5.  This occurs if other
         rules than DER are applied to DigestInfo (e.g., an indefinite
         length encoding of the underlying SEQUENCE type).  While this is
         unlikely to be a concern in practice, a cautious implementor may
         choose to employ a verification operation based on a BER decoding
         operation as specified in PKCS #1 v1.5.  In this manner,
         compatibility with any valid implementation based on PKCS #1 v1.5
         is obtained.  Such a verification operation should indicate
         whether the underlying BER encoding is a DER encoding and hence
         whether the signature is valid with respect to the specification
         given in this document.
"fixed": |-
  1.  In version 1.5 of this document, T was defined as the BER encoding, rather than the DER encoding, of the DigestInfo value. In particular, it is possible -- at least in theory -- that the verification operation defined in this document (as well as in version 2.0) rejects a signature that is valid with respect to the specification given in PKCS #1 v1.5.  This occurs if other rules than DER are applied to DigestInfo (e.g., an indefinite length encoding of the underlying SEQUENCE type).  While this is unlikely to be a concern in practice, a cautious implementor may choose to employ a verification operation based on a BER decoding operation as specified in PKCS #1 v1.5.  In this manner, compatibility with any valid implementation based on PKCS #1 v1.5 is obtained.  Such a verification operation should indicate whether the underlying BER encoding is a DER encoding and hence whether the signature is valid with respect to the specification given in this document.
"ko": |-
  1. 이 문서의 버전 1.5에서는 T가 DigestInfo 값의 DER 인코딩이 아닌 BER 인코딩으로 정의되었습니다. 특히, 적어도 이론상으로는 이 문서(및 버전 2.0)에 정의된 확인 작업이 PKCS #1 v1.5에 제공된 사양과 관련하여 유효한 서명을 거부하는 것이 가능합니다. 이는 DER 이외의 다른 규칙이 DigestInfo에 적용되는 경우에 발생합니다(예: 기본 SEQUENCE 유형의 무한 길이 인코딩). 이것이 실제로 문제가 될 가능성은 낮지만, 신중한 구현자는 PKCS #1 v1.5에 지정된 대로 BER 디코딩 작업을 기반으로 검증 작업을 채택하도록 선택할 수 있습니다. 이러한 방식으로 PKCS #1 v1.5를 기반으로 하는 모든 유효한 구현과의 호환성이 확보됩니다. 이러한 검증 작업은 기본 BER 인코딩이 DER 인코딩인지 여부와 서명이 이 문서에 제공된 사양과 관련하여 유효한지 여부를 나타내야 합니다.
