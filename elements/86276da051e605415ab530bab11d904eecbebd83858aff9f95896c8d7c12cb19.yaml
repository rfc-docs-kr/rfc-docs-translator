"hash": |-
  86276da051e605415ab530bab11d904eecbebd83858aff9f95896c8d7c12cb19
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     If a Printer supports more than one communication channel, some or
     all of those channels might support and/or require different security
     mechanisms.  In such cases, an Administrator could expose the
     simultaneous support for these multiple communication channels as
     multiple URIs for a single Printer where each URI represents one of
     the communication channels to the Printer.  To support this
     flexibility, the IPP Printer object type defines a multi-valued
     identification attribute called the "printer-uri-supported" attribute
     that MUST contain at least one URI.  The "printer-uri-supported"
     attribute has two companion attributes, the "uri-security-supported"
     attribute and the "uri-authentication-supported" attribute.  Both
     have the same cardinality as "printer-uri-supported".  The purpose of
"fixed": |-
  If a Printer supports more than one communication channel, some or all of those channels might support and/or require different security mechanisms.  In such cases, an Administrator could expose the simultaneous support for these multiple communication channels as multiple URIs for a single Printer where each URI represents one of the communication channels to the Printer.  To support this flexibility, the IPP Printer object type defines a multi-valued identification attribute called the "printer-uri-supported" attribute that MUST contain at least one URI.  The "printer-uri-supported" attribute has two companion attributes, the "uri-security-supported" attribute and the "uri-authentication-supported" attribute.  Both have the same cardinality as "printer-uri-supported".  The purpose of
"ko": |-
  프린터가 둘 이상의 통신 채널을 지원하는 경우 해당 채널 중 일부 또는 전체가 다른 보안 메커니즘을 지원 및/또는 요구할 수 있습니다. 이러한 경우 관리자는 각 URI가 프린터에 대한 통신 채널 중 하나를 나타내는 단일 프린터에 대한 여러 URI로 이러한 여러 통신 채널에 대한 동시 지원을 노출할 수 있습니다. 이러한 유연성을 지원하기 위해 IPP 프린터 객체 유형은 최소한 하나의 URI를 포함해야 하는 "printer-uri-supported" 속성이라는 다중 값 식별 속성을 정의합니다. "printer-uri-supported" 속성에는 "uri-security-supported" 속성과 "uri-authentication-supported" 속성이라는 두 가지 동반 속성이 있습니다. 둘 다 "printer-uri-supported"와 동일한 카디널리티를 갖습니다. 목적
