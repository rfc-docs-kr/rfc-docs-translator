"hash": |-
  17c265bac9f192c5e9e9cee79b5d52de1322b4498c58771e3a9841c7453898a6
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     The TEARDOWN client-to-server request stops the stream delivery for
     the given URI, freeing the resources associated with it.  A TEARDOWN
     request can be performed on either an aggregated or a media control
     URI.  However, some restrictions apply depending on the current
     state.  The TEARDOWN request MUST contain a Session header indicating
     to what session the request applies.  The TEARDOWN request MUST NOT
     include a Terminate-Reason header.
"fixed": |-
  The TEARDOWN client-to-server request stops the stream delivery for the given URI, freeing the resources associated with it.  A TEARDOWN request can be performed on either an aggregated or a media control URI.  However, some restrictions apply depending on the current state.  The TEARDOWN request MUST contain a Session header indicating to what session the request applies.  The TEARDOWN request MUST NOT include a Terminate-Reason header.
"ko": |-
  TEARDOWN 클라이언트-서버 요청은 지정된 URI에 대한 스트림 전달을 중지하고 이와 관련된 리소스를 해제합니다. TEARDOWN 요청은 집계된 URI 또는 ​​미디어 제어 URI에서 수행될 수 있습니다. 단, 현재 상태에 따라 일부 제한사항이 적용됩니다. TEARDOWN 요청에는 요청이 적용되는 세션을 나타내는 세션 헤더가 포함되어야 합니다. TEARDOWN 요청에는 Terminate-Reason 헤더가 포함되어서는 안 됩니다.
