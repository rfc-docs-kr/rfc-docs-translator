"hash": |-
  7a571272f4325e3cc41789724e70cd53be63dd78bef132bc1af66f5298e9e8c1
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
           Otherwise, the proxy MUST forward a response from the responses
           stored in the response context.  It MUST choose from the 6xx
           class responses if any exist in the context.  If no 6xx class
           responses are present, the proxy SHOULD choose from the lowest
           response class stored in the response context.  The proxy MAY
           select any response within that chosen class.  The proxy SHOULD
"fixed": |-
  - Otherwise, the proxy MUST forward a response from the responses stored in the response context.  It MUST choose from the 6xx class responses if any exist in the context.  If no 6xx class responses are present, the proxy SHOULD choose from the lowest response class stored in the response context.  The proxy MAY select any response within that chosen class.  The proxy SHOULD
"ko": |-
  - 그렇지 않으면 프록시는 응답 컨텍스트에 저장된 응답의 응답을 전달해야 합니다. 컨텍스트에 존재하는 경우 6xx 클래스 응답 중에서 선택해야 합니다. 6xx 클래스 응답이 없으면 프록시는 응답 컨텍스트에 저장된 가장 낮은 응답 클래스에서 선택해야 합니다. 프록시는 선택한 클래스 내의 모든 응답을 선택할 수 있습니다. 프록시는 다음과 같습니다.
