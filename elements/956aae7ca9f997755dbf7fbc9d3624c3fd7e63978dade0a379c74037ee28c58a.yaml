"hash": |-
  956aae7ca9f997755dbf7fbc9d3624c3fd7e63978dade0a379c74037ee28c58a
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     This RECOMMENDED operation is identical to the Send-Document
     operation (see Section 4.3.1), except that a Client MUST supply a URI
     reference ("document-uri" operation attribute) rather than the
     Document data itself.  If a Printer supports this operation, Clients
     can use both Send-URI and Send-Document operations to add new
     Documents to an existing Job.  However, if a Client needs to indicate
     that the previous Send-URI or Send-Document was the last Document,
     the Client MUST use the Send-Document operation with no Document data
     and the "last-document" flag set to 'true' (rather than using a
     Send-URI operation with no "document-uri" operation attribute).
"fixed": |-
  This RECOMMENDED operation is identical to the Send-Document operation (see Section 4.3.1), except that a Client MUST supply a URI reference ("document-uri" operation attribute) rather than the Document data itself.  If a Printer supports this operation, Clients can use both Send-URI and Send-Document operations to add new Documents to an existing Job.  However, if a Client needs to indicate that the previous Send-URI or Send-Document was the last Document, the Client MUST use the Send-Document operation with no Document data and the "last-document" flag set to 'true' (rather than using a Send-URI operation with no "document-uri" operation attribute).
"ko": |-
  이 RECOMMENDED 작업은 클라이언트가 문서 데이터 자체가 아닌 URI 참조("document-uri" 작업 속성)를 제공해야 한다는 점을 제외하면 Send-Document 작업(섹션 4.3.1 참조)과 동일합니다. 프린터가 이 작업을 지원하는 경우 클라이언트는 Send-URI 및 Send-Document 작업을 모두 사용하여 기존 작업에 새 문서를 추가할 수 있습니다. 그러나 클라이언트가 이전 Send-URI 또는 ​​Send-Document가 마지막 Document임을 표시해야 하는 경우 클라이언트는 Document 데이터가 없고 "last-document" 플래그가 'true'로 설정된 Send-Document 작업을 사용해야 합니다(MUST). "document-uri" 작업 속성 없이 Send-URI 작업을 사용하는 대신).
