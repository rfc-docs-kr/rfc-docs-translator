"hash": |-
  247831be167a3d589078c3e2c208cb10d85d56b005bc720b682f4077230d1e35
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     Note: Since the auto-sensing algorithm is probabilistic, if the
     Client requests both auto-sensing ("document-format" set to
     'application/octet-stream') and true fidelity
     ("ipp-attribute-fidelity" set to 'true'), the Printer might not be
     able to guarantee exactly what the End User intended (the
     auto-sensing algorithm might mistake one Document format for
     another), but it is able to guarantee that its auto-sensing mechanism
     will be used.
"fixed": |-
  Note: Since the auto-sensing algorithm is probabilistic, if the Client requests both auto-sensing ("document-format" set to 'application/octet-stream') and true fidelity ("ipp-attribute-fidelity" set to 'true'), the Printer might not be able to guarantee exactly what the End User intended (the auto-sensing algorithm might mistake one Document format for another), but it is able to guarantee that its auto-sensing mechanism will be used.
"ko": |-
  참고: 자동 감지 알고리즘은 확률적이므로 클라이언트가 자동 감지('application/octet-stream'으로 설정된 'document-format')와 true fidelity('true로 설정된 'ipp-attribute-fidelity')를 모두 요청하는 경우 '), 프린터는 최종 사용자가 의도한 것을 정확하게 보장할 수는 없지만(자동 감지 알고리즘은 한 문서 형식을 다른 문서 형식으로 착각할 수 있음) 자동 감지 메커니즘이 사용된다는 것을 보장할 수 있습니다.
