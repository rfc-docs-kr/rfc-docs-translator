"hash": |-
  e0dfe53003dede30fa4f1dc155fda9c1b38777536fa89c52db2680c06d984f30
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     RTSP agents can send requests to multiple different destinations,
     either server or client contexts over the same connection to a proxy.
     Then, the proxy forks the message to the different destinations over
     proxy-to-agent connections.  In these cases when multiple requests
     are outstanding, the requesting agent MUST be ready to receive the
     responses out of order compared to the order they where sent on the
     connection.  The order between multiple messages for each destination
     will be maintained; however, the order between response from
     different destinations can be different.
"fixed": |-
  RTSP agents can send requests to multiple different destinations, either server or client contexts over the same connection to a proxy. Then, the proxy forks the message to the different destinations over proxy-to-agent connections.  In these cases when multiple requests are outstanding, the requesting agent MUST be ready to receive the responses out of order compared to the order they where sent on the connection.  The order between multiple messages for each destination will be maintained; however, the order between response from different destinations can be different.
"ko": |-
  RTSP 에이전트는 프록시에 대한 동일한 연결을 통해 서버 또는 클라이언트 컨텍스트 등 여러 다른 대상으로 요청을 보낼 수 있습니다. 그런 다음 프록시는 프록시-에이전트 연결을 통해 메시지를 다른 대상으로 분기합니다. 여러 요청이 처리되지 않은 경우 요청 에이전트는 연결에서 보낸 순서와 비교하여 순서가 다른 응답을 수신할 준비가 되어 있어야 합니다. 각 대상에 대한 여러 메시지 간의 순서는 유지됩니다. 그러나 다른 대상의 응답 순서는 다를 수 있습니다.
