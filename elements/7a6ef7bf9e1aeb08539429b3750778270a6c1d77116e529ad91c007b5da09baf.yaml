"hash": |-
  7a6ef7bf9e1aeb08539429b3750778270a6c1d77116e529ad91c007b5da09baf
"type": |-
  Content
"comment": !!null |-
  null
"original": |2-
     When a response is received by an element, it first tries to locate a
     client transaction (Section 17.1.3) matching the response.  If none
     is found, the element MUST process the response (even if it is an
     informational response) as a stateless proxy (described below).  If a
     match is found, the response is handed to the client transaction.
"fixed": |-
  When a response is received by an element, it first tries to locate a client transaction (Section 17.1.3) matching the response.  If none is found, the element MUST process the response (even if it is an informational response) as a stateless proxy (described below).  If a match is found, the response is handed to the client transaction.
"ko": |-
  요소가 응답을 받으면 먼저 응답과 일치하는 클라이언트 트랜잭션(17.1.3 단원)을 찾으려고 시도합니다. 아무 것도 발견되지 않으면 요소는 응답(정보 응답인 경우에도)을 무상태 프록시(아래 설명)로 처리해야 합니다. 일치하는 항목이 발견되면 응답이 클라이언트 트랜잭션으로 전달됩니다.
