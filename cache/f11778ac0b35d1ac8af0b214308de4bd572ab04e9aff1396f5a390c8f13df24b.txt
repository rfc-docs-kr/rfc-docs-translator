   2. 클라이언트가 이미 /host/ 및 포트 /port/ 쌍으로 식별되는 원격 호스트(IP 주소)에 대한 WebSocket 연결을 갖고 있는 경우, 원격 호스트가 다른 이름으로 알려지더라도 클라이언트는 해당 연결이 완료될 때까지 기다려야 합니다. 설정되었거나 해당 연결이 실패했습니다. CONNECTING 상태에는 연결이 하나만 있어야 합니다. 동일한 IP 주소에 대한 여러 연결이 동시에 시도되는 경우 클라이언트는 다음 단계를 통해 한 번에 하나 이상의 연결이 실행되지 않도록 연결을 직렬화해야 합니다.