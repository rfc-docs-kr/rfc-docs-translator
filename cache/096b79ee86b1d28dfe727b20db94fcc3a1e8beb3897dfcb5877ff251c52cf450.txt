   DTLS 구현이 다음 예상 핸드셰이크 메시지 시퀀스 번호에 해당하는 핸드셰이크 메시지 조각을 수신하면 전체 핸드셰이크 메시지를 가질 때까지 버퍼링하거나 메시지의 순서 있는 부분을 처리하여 이를 처리해야 합니다. 기록은 완전한 TLS 핸드셰이크 메시지(필요에 따라 재구성됨)로 구성됩니다. Handshake 구조를 생성하려면 message_seq, fragment_offset 및 fragment_length 필드를 제거해야 합니다.