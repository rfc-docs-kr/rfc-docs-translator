   PVQ 벡터의 디코딩은 decode_pulses()(cwrs.c)에서 구현됩니다. 고유 코드워드 인덱스는 0과 V(N,K)-1 사이에서 균일하게 분포된 정수 값으로 디코딩됩니다. 여기서 V(N,K)는 N 샘플에서 K 펄스의 가능한 조합 수입니다. 그런 다음 인덱스는 [PVQ]에 지정된 것과 동일한 방식으로 벡터로 변환됩니다. 인덱싱은 V(N,K)([PVQ]에서 N(L,K)로 표시됨) 계산을 기반으로 합니다.